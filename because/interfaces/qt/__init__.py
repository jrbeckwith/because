"""Interface using PyQt.

This interface should work with either PyQt4 or PyQt5. It should also allow use
of Qt's signals and slots system, which also allows asynchronous operation and
multithreading. However, it does not offer anything to specifically help with
QGIS.
"""
