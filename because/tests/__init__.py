"""Tests for because.

The tests are included inside the package so that they can be run in any
environment where because is failing, allowing us to blame the environment
instead of our code if that environment is producing unexpected test failures.
"""
